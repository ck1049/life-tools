spring:
  application:
    name: oss-service
  cloud:
    nacos:
      config:
        name: ${spring.application.name}
        server-addr: 127.0.0.1:8848
        namespace: life-tools
        username: nacos
        password: nacos
        group: DEFAULT_GROUP